@model DTKTLockViewModel
@{
    ViewBag.Title = L("DuToanKT.Create");
}
@section Styles{
}
@*<div id="editor" class="modal" role="dialog" tabindex="-1">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close btn-close-modal" data-dissmiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                    <h4>@L("DTKT.LockHeader")</h4>
                </div>
                <div class="modal-body">

                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-mvc btn-mvc-green">@L("Buttons.SaveChanges")</button>
                    <button type="button" class="btn btn-mvc btn-close-modal" data-dissmiss="modal">@L("Buttons.Close")</button>
                </div>
            </div>
        </div>
    </div>*@
@using (Html.BeginForm("Update", "Lock", FormMethod.Post, new { @class = "form-horizontal" }))
{
    @Html.AntiForgeryToken()

    @Html.HiddenFor(x => x.Id)

    <div class="form-group">
        <div class="col-sm-3">
            @Html.LabelFor(x => x.TenDonVi, new { @class = "control-label-r" })
        </div>
        <div class="col-sm-7">
            @Html.HiddenFor(x => x.Id_DonVi)
            @Html.TextBoxFor(x => x.TenDonVi, new { @class = "form-control", @readonly = true })
        </div>
    </div>

    <div class="form-group">
        <div class="col-sm-3">
            @Html.LabelFor(x => x.Id_PhongBan, new { @class = "control-label-r" })
        </div>
        <div class="col-sm-9">
            @Html.TextBoxFor(x => x.Id_PhongBan, new { @class = "form-control" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-sm-3">
            @Html.LabelFor(x => x.Id_User, new { @class = "control-label-r" })
        </div>
        <div class="col-sm-9">
            @Html.TextBoxFor(x => x.Id_User, new { @class = "form-control" })
        </div>
    </div>

    <div class="form-group">
        <div class="col-sm-3">
            @Html.LabelFor(x => x.GhiChu, new { @class = "control-label-r" })
        </div>
        <div class="col-sm-9">
            @Html.TextAreaFor(x => x.GhiChu, new { @class = "form-control", @rows = 4 })
        </div>
    </div>
}
@section Scripts{
}
